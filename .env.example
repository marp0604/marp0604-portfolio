# Copia este archivo como .env.local y rellena los valores
# NUNCA subas .env.local al repositorio

# Ejemplo de variable pública (accesible en el cliente)
# PUBLIC_SITE_URL=https://marp0604.com

# Ejemplo de variable privada (solo servidor)
# CONTACT_API_KEY=tu_api_key_aqui
